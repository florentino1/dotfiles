- defaults:
    link:
      relink: true

- clean: ['~']
  
- link:
    ~/.zshrc:
      force: true
      path: zshrc

    ~/.bashrc:
    ~/.shell:
    
    ~/.zsh:
      force: true
      path: zsh
      
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.hammerspoon:
    ~/.lldbinit:
    

#- shell:
#  - command: chsh -s $(which zsh)
#    stdin: true
#    stdout: true
#   stderr: true
  

  #- command: sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
  #- command: $(which bash) -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"



